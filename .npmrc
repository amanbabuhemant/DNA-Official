# Force npm to install optional dependencies
install-optional=true

# Use exact package-lock.json
package-lock=true

# Disable legacy peer deps warnings
legacy-peer-deps=false

# Engine strict to ensure compatibility
engine-strict=true

# Improve resolution for platform-specific packages
prefer-online=true
